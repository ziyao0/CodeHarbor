spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
config:
  gateway:
    skip-apis:
      - /user-center-service/usercenter/app/list
      - /user-center-service/auth/login
